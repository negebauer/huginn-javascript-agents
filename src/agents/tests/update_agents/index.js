const agentInfo1 = require('./agentInfo1.json')
const agentInfo2 = require('./agentInfo2.json')
const deployInfo = require('./deployInfo.json')
const generateEvent = require('./generateEvent')

module.exports = {
  agentInfo1,
  agentInfo2,
  deployInfo,
  generateEvent,
}
